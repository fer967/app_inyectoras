<h1>Dashboard de Fallas</h1>

<div id="chart"></div>

{{!-- si no uso la libreria de forma local uso cdn (ver) --}}
<script src="https://cdn.jsdelivr.net/npm/apexcharts"></script>
<script>
    // Obtener las estadísticas del backend
    const estadisticas = {{{ estadisticas }}};
    //console.log('Estadísticas recibidas en el frontend:', estadisticas);  // ver !!!
    // Configurar el gráfico de ApexCharts
    var options = {
        chart: {
            type: 'bar'
        },
        series: [{
            name: 'Cantidad de Fallas',
            data: Object.values(estadisticas.fallasPorMarca)
        }],
        xaxis: {
            categories: Object.keys(estadisticas.fallasPorMarca)
        }
    }
    // Crear el gráfico
    var chart = new ApexCharts(document.querySelector("#chart"), options);
    // Renderizar el gráfico
    chart.render();
</script>
<a href="/">Volver a Inicio</a>


{{!-- <h1>Ingresar Reparación de Inyectora</h1>

<form action="/consultar/ingresar-reparacion" method="post">
    <div class="form-group">
        <label for="marca">Marca:</label>
        <input type="text" class="form-control" id="marca" name="marca" required>
    </div>
    <div class="form-group">
        <label for="modelo">Modelo:</label>
        <input type="text" class="form-control" id="modelo" name="modelo" required>
    </div>
    <div class="form-group">
        <label for="falla">Falla:</label>
        <textarea class="form-control" id="falla" name="falla" rows="3"></textarea>
    </div>
    <div class="form-group">
        <label for="sistema">Sistema:</label>
        <select class="form-control" id="sistema" name="sistema" required>
            <option value="">Seleccionar...</option>
            <option value="hidraulico">Hidráulico</option>
            <option value="electrico">Eléctrico</option>
            <option value="mecanico">Mecánico</option>
            <option value="inyeccion">Inyección</option>
            <option value="refrigeracion">Refrigeración</option>
            <option value="calefaccion">Calefacción</option>
        </select>
    </div>

    <div class="form-group">
        <label for="reparacion_realizada">Reparación Realizada:</label>
        <textarea class="form-control" id="reparacion_realizada" name="reparacion_realizada" rows="3"></textarea>
    </div>
    <div class="form-group">
        <label for="operario_nombre">Nombre del Operario:</label>
        <input type="text" class="form-control" id="operario_nombre" name="operario_nombre" required>
    </div>
    <div class="form-group">
        <label for="operario_apellido">Apellido del Operario:</label>
        <input type="text" class="form-control" id="operario_apellido" name="operario_apellido" required>
    </div>
    <div class="form-group">
        <label for="fecha">Fecha:</label>
        <input type="date" class="form-control" id="fecha" name="fecha" required>
    </div>
    <button type="submit" class="btn btn-primary">Guardar Reparación</button>
</form>
<br>
<a href="/consultar">Volver</a> --}}

{{!-- <h1>Detalle de la Tarea</h1>

<div class="card mt-3 p-3">
    <h3>{{tarea.titulo}}</h3>
    <p><strong>Descripción:</strong> {{tarea.descripcion}}</p>
    <p><strong>Asignada a:</strong> {{tecnico.nombre}} {{tecnico.apellido}}</p>
    <p><strong>Estado:</strong> 
        <span class="badge bg-info text-dark">{{tarea.estado}}</span>
    </p>
    <p><strong>Hora de comienzo:</strong>
        {{#if tarea.hora_inicio}}
            {{formatDate tarea.hora_inicio "DD/MM/YYYY HH:mm"}}
        {{else}}
            No registrada
        {{/if}}
    </p>
    <p><strong>Hora de finalización:</strong>
        {{#if tarea.hora_fin}}
            {{formatDate tarea.hora_fin "DD/MM/YYYY HH:mm"}}
        {{else}}
            Aún sin finalizar
        {{/if}}
    </p>
    <p><strong>Total horas:</strong>
        {{#if tarea.horas_totales}}
        {{tarea.horas_totales}} hs
        {{else}}
            -
        {{/if}}
    </p>
</div>
<hr>
{{!-- Si la tarea aún está en progreso mostramos el formulario --}}
{{#if (eq tarea.estado "en_progreso")}}
<form action="/consultar/tareas/finalizar/{{tarea.id}}" method="POST" class="mt-4">
    <div class="form-group mb-3">
        <label for="detalle_finalizacion"><strong>Detalle final de la tarea:</strong></label>
        <textarea name="detalle_finalizacion" id="detalle_finalizacion"
            class="form-control" rows="3" required></textarea>
    </div>
    <button type="submit" class="btn btn-success">
        Finalizar Tarea
    </button>
</form>
{{else}}
<p class="alert alert-success mt-4">Esta tarea ya fue finalizada.</p>
{{/if}}
<br>
<a href="/consultar/tareas" class="btn btn-secondary">Volver</a> --}}
